<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.xcu.mapper.UserFileMapper">

    <update id="updateBatchFolderId">
        update
            easypan.tb_user_file uf
        set
            uf.file_pid = #{filePid}
        where
            uf.user_id = #{userId}
            and uf.file_id in
            <foreach collection="fileId" item="item" separator="," open="(" close=")">
                #{item}
            </foreach>
    </update>

    <update id="updateBatchStatus">
        update
            easypan.tb_user_file uf
        set
            uf.status = #{status},
            uf.recovery_time = #{recoveryTime}
        where
            uf.user_id = #{userId}
            and uf.file_id in
            <foreach collection="fileIds" item="item" separator="," open="(" close=")">
                #{item}
            </foreach>
    </update>


</mapper>